---
name: ab-test-setup
description: "当用户想规划、设计或评估 A/B 测试、分流实验或增长实验体系时使用。适合需要明确假设、指标、样本量、测试时长和结果判断的人。页面转化思路看 `page-cro`，埋点与测量实现看 `analytics-tracking`。"
metadata:
  version: 1.2.0
---

# A/B 测试设计

这是 `ab-test-setup` 的中文本地化说明版。它不是逐句硬译，而是把原始 `SKILL.md` 整理成更适合中文用户直接执行的工作手册；需要逐段对照英文原版时，请同时查看同目录下的 `SKILL.md`。

## 适用场景

当用户想规划、设计或评估 A/B 测试、分流实验或增长实验体系时使用。适合需要明确假设、指标、样本量、测试时长和结果判断的人。页面转化思路看 `page-cro`，埋点与测量实现看 `analytics-tracking`。

## 使用方式

- 先读取 `.agents/product-marketing-context.md`（或旧路径 `.claude/product-marketing-context.md`），避免重复提问并沿用已有定位信息。
- 围绕“初始评估”推进任务，并在需要时回看原版 `Initial Assessment` 小节获取更细颗粒度指引。
- 围绕“核心原则”推进任务，并在需要时回看原版 `Core Principles` 小节获取更细颗粒度指引。
- 围绕“假设框架”推进任务，并在需要时回看原版 `Hypothesis Framework` 小节获取更细颗粒度指引。
- 围绕“测试类型”推进任务，并在需要时回看原版 `Test Types` 小节获取更细颗粒度指引。

## 重点关注

原版技能里最值得优先对照的细项通常包括：
- `1. Start with a Hypothesis`
- `2. Test One Thing`
- `3. Statistical Rigor`
- `4. Measure What Matters`
- `Structure`
- `Example`
- `Quick Reference`
- `Primary Metric`

## 典型交付物

- 实验假设
- 指标与样本量建议
- 结构化测试计划

## 原始章节导航

- `初始评估`（原版：`Initial Assessment`）
- `核心原则`（原版：`Core Principles`）
- `假设框架`（原版：`Hypothesis Framework`）
- `测试类型`（原版：`Test Types`）
- `样本量`（原版：`Sample Size`）
- `指标选择`（原版：`Metrics Selection`）
- `设计变体`（原版：`Designing Variants`）
- `流量分配`（原版：`Traffic Allocation`）
- `实现方式`（原版：`Implementation`）
- `运行测试`（原版：`Running the Test`）
- `结果分析`（原版：`Analyzing Results`）
- `文档沉淀`（原版：`Documentation`）

## 相关技能

- `page-cro`
- `analytics-tracking`
- `copywriting`

## 使用建议

- 如果你是第一次在这个仓库里使用该技能，先看中文版本建立整体认知，再按需回看英文原版中的细节、模板和边界条件。
- 如果任务涉及多技能串联，优先遵守原版 `Related Skills` 和交叉引用中的边界说明，避免把一个技能的职责做得过宽。
